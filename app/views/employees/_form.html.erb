<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
        <title>Bootstrap 101 Template</title>

        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap-theme.min.css" integrity="sha384-rHyoN1iRsVXV4nD0JutlnGaslCJuC7uwjduW9SVrLvRYooPp2bWYgmgJQIXwl/Sp" crossorigin="anonymous">
    </head>
  <body>
  <div style="padding: 10px;">  
<%= form_with model: employee do |form| %>
    <div class="form-group">
        <%= form.label :employee_number %>
        <%= form.text_field :employee_number, id: "empNumber", class: "form-control", placeholder: "Employee Number" %>
    </div>
    <!-- Validation of Employee Number -->
    <% employee.errors.full_messages_for(:employee_number).each do |message| %>
        <div> <%= message %> </div>
    <% end %>

    <div class="form-group">
        <%= form.label :last_name %>
        <%= form.text_field :last_name, id: "lastName", class: "form-control", placeholder: "Enter Last Name" %>
    </div>
    <!-- Validation for Last Name -->
    <% employee.errors.full_messages_for(:last_name).each do |message| %>
        <div> <%= message %> </div>
    <% end %>

    <div class="form-group">
        <%= form.label :first_name %>
        <%= form.text_field :first_name, id: "firstName", class: "form-control", placeholder: "Enter First Name" %>
    </div>
    <!-- Validation for First Name -->
    <% employee.errors.full_messages_for(:first_name).each do |message| %>
        <div> <%= message %> </div>
    <% end %>

    <div class="form-group">
        <%= form.label :middle_name %>
        <%= form.text_field :middle_name, id: "middleName", class: "form-control", placeholder: "Enter Middle Name" %>
    </div>
    <!-- Validation for Middle Name -->
    <% employee.errors.full_messages_for(:middle_name).each do |message| %>
        <div> <%= message %> </div>
    <% end %>

    <div class="form-group">
        <%= form.label :birthday %>
        <%= form.text_field :birthday, id: "birthday", class: "form-control", placeholder: "DD-MMM-YYYY (e.g 13-DEC-1989" %>
    </div>
    <!-- Validation for Birthday -->
    <% employee.errors.full_messages_for(:birthday).each do |message| %>
        <div> <%= message %> </div>
    <% end %>

    <div class="form-group">
        <%= form.label :daily_rate %>
        <%= form.text_field :daily_rate, id: "daily_rate", class: "form-control", placeholder: "PHP XXXX.XX" %>
    </div>
    <!-- Validation for Daily Rate -->
    <% employee.errors.full_messages_for(:daily_rate).each do |message| %>
        <div> <%= message %> </div>
    <% end %>

    <div class="form-group">
        <%= form.label :working_days %>
        <%= form.text_field :working_days, id: "workingDays", class: "form-control", placeholder: "WFS or TTHS" %>
    </div>
    <% employee.errors.full_messages_for(:working_days).each do |message| %>
        <div> <%= message %> </div>
    <% end %>
    
    <div>
        <%= form.submit %>
    </div>
    <% end %>
</div>    

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
  </body>
</html>